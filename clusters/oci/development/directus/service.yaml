apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: directus
    app.kubernetes.io/name: directus
  name: directus
  namespace: development
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8055
  selector:
    app: "directus"
  type: ClusterIP
